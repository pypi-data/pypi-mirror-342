# Rename this file to .env and fill in your actual values

# PostgreSQL Database URL
# Format: postgresql+asyncpg://<user>:<password>@<host>:<port>/<database_name>
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/cyberwave_db

# Optional: Secret key for JWT tokens (if using authentication)
# Generate a strong key using: openssl rand -hex 32
# SECRET_KEY=your_super_secret_key

# Optional: Allowed origins for CORS (comma-separated)
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,https://your-frontend-domain.com 