[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "m24842_models"
version = "0.1.7"
description = ""
requires-python = ">=3.8"
dependencies = [
    "torch",
    "einops",
    "rotary_embedding_torch",
]