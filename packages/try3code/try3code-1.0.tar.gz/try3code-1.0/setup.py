from setuptools import setup, find_packages

setup(
    name='try3code',
    version='1.0',
    packages=find_packages(),
    include_package_data=True,
    description='A custom code reader from text files',
    author='Your Name',
)
