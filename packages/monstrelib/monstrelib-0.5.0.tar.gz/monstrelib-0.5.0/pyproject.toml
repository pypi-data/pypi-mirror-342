[project]
name = "monstrelib"
description = "Une mini bibliothèque qui fait des additions comme un monstre"
authors = [
    { name="Mohamed Echcharif EL JAZOULI", email="charif.eljazouli@email.com" }
]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.7"
dynamic = ["version"]
dependencies = []


[project.urls]
Homepage = "https://github.com/Charifjaz/monstrelib"

[build-system]
requires = ["setuptools>=61.0","setuptools-scm"]
build-backend = "setuptools.build_meta"

[project.optional-dependencies]
dev = ["pytest", "pytest-cov", "pre-commit"]

[project.scripts]
monsterlib = "monsterlib.cli:main"

[tool.setuptools_scm]
version_scheme = "post-release"
local_scheme = "no-local-version"
