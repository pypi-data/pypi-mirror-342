from setuptools import setup, find_packages

setup(
    name='sklearne',
    version='1.0',
    packages=find_packages(),
    include_package_data=True,
    description='Saver',
    author='Your Name',
)
