[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "django-contentor-video-processor"
version = "0.1.0"
description = "A reusable Django app for integrating video processing capabilities to your video models"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [
  { name="Your Name", email="your@email.com" }
]
dependencies = [
  "django>=3.2"
]

[project.urls]
Homepage = "https://github.com/yourusername/django-contentor-video-processor"
