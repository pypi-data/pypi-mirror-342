[project]
name = "persistence-client"
version = "0.1.8"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aio-pika>=9.5.5",
    "faststream>=0.5.39",
    "pydantic>=2.11.3",
]

[project.scripts]
persistence-client = "persistence_client:main"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
