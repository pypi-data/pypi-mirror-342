[project]
name = "encode-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "yangyanxing", email = "yangyanxing@360.cn" }
]
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.6.0",
]

[project.scripts]
encode-mcp-server = "encode_mcp_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[[tool.uv.index]]
url = "https://mirrors.tuna.tsinghua.edu.cn/pypi/web/simple"
