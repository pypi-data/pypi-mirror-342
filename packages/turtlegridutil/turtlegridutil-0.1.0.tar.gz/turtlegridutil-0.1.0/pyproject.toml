[project]
name = "turtlegridutil"
version = "0.1.0"
description = "The utility for drawing coordinate grid on python turtle's screen."
authors = [
    {name = "Vasyl Boliukh",email = "vskesha@gmail.com"}
]
license = "MIT"
repository = "https://github.com/Vskesha/TurtleGridUtil"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
]

[tool.poetry.dependencies]
python = ">=3.8"


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
