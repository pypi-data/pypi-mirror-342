[project]
name = "t4wefan-code-scanner"
version = "0.1.3"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "toml",
]

[project.scripts]
scanner = "scanner.run_code_scanner:main"
