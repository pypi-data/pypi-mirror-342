[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "canoll"
version = "1.0.0"
description = "Library array ringan tanpa NumPy."
authors = [{name = "xycan", email = "eternals-tolong@gmail.com"}]
license = {text = "Eternals Vlazars License"}
