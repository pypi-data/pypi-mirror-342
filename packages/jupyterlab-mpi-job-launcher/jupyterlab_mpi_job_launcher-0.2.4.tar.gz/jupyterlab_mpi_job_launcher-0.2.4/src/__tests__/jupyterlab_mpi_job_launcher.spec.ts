/**
 * Example of [Jest](https://jestjs.io/docs/getting-started) unit tests
 */

describe('jupyterlab-mpi-job-launcher', () => {
  it('should be tested', () => {
    expect(1 + 1).toEqual(2);
  });
});
