[build-system]
requires = ["setuptools>=65.6.2", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "onetokenpy"
version = "0.1.0"
description = "A library for running local LLM classification tasks on data"
readme = "README.md"
requires-python = ">=3.8"

[tool.setuptools]
license-files = [] 