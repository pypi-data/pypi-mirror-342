[project]
name = "fluxiondb"
version = "0.0.1"
description = "Python client for Fluxion"
authors = [
    {name = "seyedasfar",email = "seyed.asfar@outlook.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[tool.poetry]
packages = [{include = "fluxiondb", from = "src"}]


[tool.poetry.group.dev.dependencies]
pytest = "^8.3.5"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
