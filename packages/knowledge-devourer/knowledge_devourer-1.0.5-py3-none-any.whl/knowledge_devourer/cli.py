import sys
from knowledge_devourer.core.config import Config
from knowledge_devourer.core.processor import process_posts, process_reels
from loguru import logger


def main():
    pass
    # TODO: Generate handler of cli arguments and call process_posts, process_reels properly.
    #  Allow full custom paths configuration. Allow file with links as input, Allow single link as input.
    #  Define helper function in knowledge_devourer.core.utils if needed.


if __name__ == "__main__":
    main()
