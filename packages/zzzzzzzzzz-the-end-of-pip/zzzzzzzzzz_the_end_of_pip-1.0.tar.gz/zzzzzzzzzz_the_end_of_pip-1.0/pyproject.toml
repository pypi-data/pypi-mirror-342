[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "zzzzzzzzzz-the-end-of-pip"
version = "1.0"
description = "the end?"
readme = "README.md"
requires-python = ">=2.0"
dependencies = []