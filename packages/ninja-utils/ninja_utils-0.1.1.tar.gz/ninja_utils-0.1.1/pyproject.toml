[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ninja-utils"
version = "0.1.1"
authors = [
    {name = "Fabio Antonio Astore", email = "astore.a.fabio@gmail.com"}
]
description = "Python Data Structure Package"
readme = "README.md"
license = {file = "LICENSE"}
dependencies = []

[tool.setuptools]
packages = ["data_structures", "algorithms"]
