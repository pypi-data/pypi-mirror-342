globalSettings:
   # Catching failed word patterns from pytest stdout
   # Example: pytest.failed('Failed: This ia a sample failed message from pytest script')
   verifyFailurePatterns: ['Failed:', 'FAILED.*::']
   
stages:
   Test:
      enable: True
      tasks:
        - PyTest-Layer3:
            enable: True
            env: DOMAIN=Communal/Samples/demoEnv1
            #loadBalanceGroup: qa
            playlist:
               - /Testcases/Samples/runPytest.yml

