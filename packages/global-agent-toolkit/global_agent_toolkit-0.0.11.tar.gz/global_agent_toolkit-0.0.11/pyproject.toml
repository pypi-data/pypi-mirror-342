[project]
name = "global-agent-toolkit"
version = "v0.0.11"
description = "..."
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["rock-solid-base>=0.0.12"]

[[project.authors]]
name = "Arthur Brenno"
email = "64020210+arthurbrenno@users.noreply.github.com"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.directories]
packages = ["gat"]

[tool.hatch.build.targets.wheel]
packages = ["gat"]

[dependency-groups]
dev = ["cerebras-cloud-sdk>=1.29.0", "google-genai>=1.11.0", "pydantic>=2.11.3"]
